# kafka-server
This project sets up a local Kafka server using Confluent Platform Docker images. It includes a docker-compose.yml file that starts the essential Kafka services (Zookeeper &amp; Kafka broker), and two Python scripts to demonstrate simple message production and consumption.  The setup is lightweight and ideal for learning or testing Kafka locally.
